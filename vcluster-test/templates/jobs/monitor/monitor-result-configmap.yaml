{{- range .Values.jobs.monitorJob.scripts }}
{{- $resourceName := (index (splitList " " .) 1) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $resourceName }}-{{ $.Values.jobs.monitorJob.script.resultConfigMapName }}
data:
  output: "EMPTY OUTPUT"
  error: "NO ERRORS"
---
{{- end }}
